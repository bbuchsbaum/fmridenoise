% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ndx_spectral.R
\name{Select_Significant_Spectral_Regressors}
\alias{Select_Significant_Spectral_Regressors}
\title{Select Spectral Regressors via Information Criterion}
\usage{
Select_Significant_Spectral_Regressors(
  y,
  U_candidates,
  criterion = c("BIC", "AIC"),
  delta_threshold = 2,
  verbose = FALSE
)
}
\arguments{
\item{y}{Numeric vector of data to be modeled (typically the mean residual
time series used to estimate the spectrum).}

\item{U_candidates}{Matrix of sine/cosine regressors. Columns should come in
pairs (sine then cosine) corresponding to frequencies.}

\item{criterion}{Character string, either "BIC" or "AIC". Default "BIC".}

\item{delta_threshold}{Numeric, minimum decrease in the chosen information
criterion required to retain a candidate pair. Default 2.}

\item{verbose}{Logical, whether to print verbose diagnostic messages.}
}
\value{
Matrix of selected regressors (or NULL if none selected). Attributes
  "freq_hz" and "freq_rad_s" are propagated for the selected frequencies.
}
\description{
Given a set of candidate sine/cosine regressors, iteratively add the pair
that yields the largest improvement in BIC (or AIC) when regressing the
provided time-series. Addition stops when no remaining pair improves the
criterion by more than `delta_threshold`.
}
\keyword{internal}
