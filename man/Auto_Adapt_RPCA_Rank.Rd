% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ndx_rpca.R
\name{Auto_Adapt_RPCA_Rank}
\alias{Auto_Adapt_RPCA_Rank}
\title{Auto-Adaptive Rank Selection for RPCA-L}
\usage{
Auto_Adapt_RPCA_Rank(
  singular_values,
  drop_ratio = 0.02,
  k_min = 20L,
  k_max = 50L
)
}
\arguments{
\item{singular_values}{Numeric vector of singular values sorted in
decreasing order.}

\item{drop_ratio}{Numeric scalar specifying the drop-off threshold
relative to the first singular value. Default is `0.02` (2%).}

\item{k_min}{Integer minimum allowable rank. Default is `20`.}

\item{k_max}{Integer maximum allowable rank. Default is `50`.}
}
\value{
Integer adaptive rank value within `[k_min, k_max]` and not
  exceeding `length(singular_values)`.
}
\description{
Determines an appropriate rank for the low-rank component of RPCA
based on the "elbow" in a provided singular value spectrum.
The first index where the singular value ratio drops below
`drop_ratio` is chosen, then clamped between `k_min` and `k_max`.
}
